<!-- Command to trigger automatic compilation for sass, when a sass file is saved -->
    npm run compile:sass

<!-- Command to trigger automatic reload for index.html in order to see last changes -->
    live-server